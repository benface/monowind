{
  "name": "@monowind/example-html",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "cdn": "pnpm -C ../../packages/core build && node copy-cdn.mjs",
    "dev": "pnpm run cdn && vite --port 5180",
    "test": "pnpm run cdn && node smoke.test.mjs"
  },
  "devDependencies": {
    "monowind": "workspace:*",
    "playwright": "^1.63.0",
    "vite": "^8.3.1"
  }
}
