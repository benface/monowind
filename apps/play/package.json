{
  "name": "@monowind/play",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -C ../../packages/core build && pnpm -C ../../packages/ascii build && node copy-cdn.mjs && vite --port 5181",
    "build": "pnpm -C ../../packages/core build && pnpm -C ../../packages/ascii build && node copy-cdn.mjs && node build.mjs",
    "test": "pnpm -C ../../packages/core build && pnpm -C ../../packages/ascii build && node copy-cdn.mjs && node smoke.test.mjs"
  },
  "dependencies": {
    "monowind": "workspace:*"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "vite": "^8.2.2"
  }
}
