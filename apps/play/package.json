{
  "name": "@monowind/play",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "cdn": "pnpm -C ../../packages/core build && pnpm -C ../../packages/ascii build && pnpm -C ../../packages/qr-code build && pnpm -C ../../packages/ui build && node copy-cdn.mjs",
    "dev": "pnpm run cdn && vite --port 5181",
    "dev:netlify": "pnpm run cdn && npx -y netlify-cli@27 dev --offline --no-open --port 8888 --functions netlify/functions --command \"vite --port 5181\" --target-port 5181",
    "build": "pnpm run cdn && node build.mjs",
    "test": "pnpm run cdn && node --test netlify/lib/short.test.mjs && node smoke.test.mjs"
  },
  "dependencies": {
    "@monowind/ascii": "workspace:*",
    "@monowind/qr-code": "workspace:*",
    "@monowind/themes": "workspace:*",
    "@monowind/ui": "workspace:*",
    "@netlify/blobs": "^11.1.1",
    "monowind": "workspace:*"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "vite": "^8.3.1"
  }
}
